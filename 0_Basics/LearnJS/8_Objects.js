/*
    1. Whatever created using a new keyword is an object in js
    2. Apart from object, arrays. Dates(), mathsd(), boolean(), Numbers are also object

    3. Date(year,months,day, hour, min,sec,ms);
    methods:-getFullYear....

    4. Maths:- maths.method()
        abs(), ceil(), floor(), sqrt(), pow(), random(), round()

    5. Number:-
        integer, MIN_VALUE, MAX_VALUE, POSITIVE_INFINITY
*/

let mydate = new Date();
let num = new Number(10);